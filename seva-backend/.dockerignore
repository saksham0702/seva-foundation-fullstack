node_modules
dist
.git
.gitignore

# Environment files — never bake secrets into images; pass via -e or --env-file at runtime
.env
.env.*

# Runtime upload data — mount a volume at /app/uploads instead
uploads

# Dev tooling — not needed in any Docker stage
nodemon.json

# Build noise
npm-debug.log*
*.tsbuildinfo

# Docker meta files themselves
Dockerfile
.dockerignore